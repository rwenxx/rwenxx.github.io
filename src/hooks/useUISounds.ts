"use client";

// Sounds have been disabled at user's request.
export function useUISounds() {
  return {
    playPaper: () => {},
    playMarker: () => {},
    playStamp: () => {},
    playDoom: () => {},
  };
}
